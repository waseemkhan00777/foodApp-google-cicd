steps:
  # Node version check
  - name: node:14
    id: Check node version
    entrypoint: node
    args: ["--version"]
  # Npm version check
  - name: node:14
    id: Check npm version
    entrypoint: npm
    args: ["--version"]
